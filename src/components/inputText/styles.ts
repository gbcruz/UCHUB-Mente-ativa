import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input : {
        width: '100%',
        height: 50,
        borderRadius: 8,
        borderWidth: 2,
        borderColor: '#a0a0a0ff',               // borda visível para teste
        padding: 12,
        fontSize: 16,
        backgroundColor: '#ffffff', // cor temporária para ver se aparece
        color: '#000000',
    },
});